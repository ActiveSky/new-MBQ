## 2026-06-30-01：wqkv rank ablation 与严格低成本 swap 实验

推荐执行版（中文，可直接复制）

/goal 在 `/home/users/Mayinyi/jikuixie/new-MBQ` 中完成 2026-06-30 第 01 批 MBQ 实验：用现有 cache 离线生成 `wqkv` SVD rank 64/32 变体，完成 actual-bit 和 Wikitext2 PPL 快筛，按门槛运行 OCRBench，并在需要时完成 `51/42` 的严格低成本 swap 修复；所有有效结果和证据写回 `MBQ_DAILY_PLAN.md`。
验证：先读取 `MBQ_DAILY_PLAN.md` 中 2026-06-29 和 2026-06-30 的 OCRBench 计划、`tmp.md` 本目标、`tools/build_custom_mbq_cache.py`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`、`tests/test_PPL/2_compute_quant_ppl.sh`；确认 `mixed_0.3` 基准为 actual bit 2.3963、OCRBench 701。为 `custom_wo_all_w2_w3_55` 和 `custom_wo_w2_w3_42` 分别生成 `wqkv_r64` 与 `wqkv_r32` cache，检查每个新 cache 的 `attention.wqkv` low-rank 项数量、`rank`、`up/down` shape、`linear_bit_map`、`low_rank_count` 和 `.summary.json`；按公式 `actual_bits = sum_i(numel(W_i) * bit_i) + sum_{i in low_rank}(numel(up_i) + numel(down_i)) * 4`、`actual bit = actual_bits / 6979321856`、`Delta MiB vs mixed0.3 = (actual_bit - 2.3963) * 832.0` 计算成本。对 4 个 rank 变体运行 Wikitext2 256-sample PPL 快筛；对符合门槛的 `r64` 变体运行 OCRBench，`r32` 只在对应 `r64` 不发生明显 OCRBench 退化且自身 PPL 不灾难性恶化时运行 OCRBench。若 rank ablation 失败或需要补充低成本点，则构造并评估 `custom_51_swap_l4w2_l14w2`、`custom_51_swap_l10w2_l17w2`、`custom_51_swap_l11w2_l18w2`、`custom_42_swap_l19w2_l24w3`、`custom_42_swap_l19w2_l23wo` 中优先级最高且 actual bit 合格的候选。每个完成候选必须在 `MBQ_DAILY_PLAN.md` 记录 cache 路径、summary 路径、PPL 日志、OCRBench 输出、actual bit、Delta MiB、OCRBench 总分和子项、状态和结论。
约束：不要重新跑完整 scale 生成流程来做 `wqkv` rank ablation；rank 变体只能从已有 `custom_wo_all_w2_w3_55.pt` 和 `custom_wo_w2_w3_42.pt` 离线裁剪 `attention.wqkv` 的 `low_rank` 因子，`wqkv` 之外的 low-rank 项保持 rank 128，`linear_bit_map` 和 3-bit 层选择保持不变。swap cache 必须从 `scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_mixed_0.0.pt` 构造，不得从非零 keep-ratio cache 或已有自定义 cache 反向修改。任何 actual bit 大于或等于 2.3963 的配置不得作为本轮主 Pareto 候选；不得把 PPL 当作主指标替代 OCRBench；不得修改量化核心算法、模型代码、数据集逻辑、baseline 结果、已有无关实验记录或非本轮 cache。
边界：允许读取和使用 `scale_cache/mbq`、`outputs`、`logs`、`MBQ_DAILY_PLAN.md`、`tmp.md`、`tools/build_custom_mbq_cache.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`。允许新增一个聚焦的离线 cache 处理脚本，例如 `tools/truncate_wqkv_low_rank.py`，以及为本轮候选写入明确命名的 `.pt`、`.summary.json`、PPL 日志、OCRBench 输出和文档记录。禁止写入或覆盖非本轮实验输出，禁止改动 `qmllm`、`main.py`、`main_quant.py`、第三方库、模型权重、数据集源码和无关配置。
迭代策略：先做 discovery 和一致性检查，再一次只处理一个候选。每次生成 cache 后先检查 summary 和 actual bit，再进入 PPL；每次长任务前先查询 GPU，可用显存约 25 GiB 即可运行 OCRBench，不要求 GPU 完全空闲。长任务优先通过 tmux 目标 pane 执行：先运行 `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'` 找到 window_index 为 0 且 pane_index 为 0 的目标 pane，发送命令用 `tmux send-keys -t "$resolved_target" '实际命令' C-m`，随后用 `tmux capture-pane` 或日志文件确认启动和结束。PPL 推荐命令形态为 `GPU_ID=可用GPU SCALE_FILE_NAME=候选文件名.pt DATASET=wikitext2 N_SAMPLES=256 bash tests/test_PPL/2_compute_quant_ppl.sh`。OCRBench 运行前更新 `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 的 `scale_path` 和 `output_path`，并更新 `configs/internvl2/scripts/2_run_quant_eval.sh` 的 `LOG_FILE` 和 `GPU_ID`，运行后读取 `outputs/my-internvl2-8b/候选名/results/ocrbench_results.txt` 和对应 results json。遇到失败先读日志和 summary，最多做 3 轮有新证据的修复；同类失败连续 3 次则暂停。
完成条件：4 个 `wqkv` rank 变体都已生成或明确说明无法安全生成，并且每个已生成变体都有 summary、actual-bit 复核和 Wikitext2 PPL 记录；符合门槛的 `r64` 变体已经完成 OCRBench，符合门槛的 `r32` 变体已经完成 OCRBench 或明确记录跳过理由；如果 rank ablation 未产生可用候选，则至少完成优先级最高的 2 个严格低成本 swap 候选的 cache 构造、actual-bit 复核、PPL 快筛和必要 OCRBench。`MBQ_DAILY_PLAN.md` 中必须包含 2026-06-30 结果表、证据路径、阶段性结论、是否替代 `55/42/51` 代表点的判断，以及下一步建议。
暂停条件：需要新增外部凭证、付费资源、生产数据、破坏性操作或用户放宽 OCRBench 阈值；`mixed_0.0.pt`、`custom_wo_all_w2_w3_55.pt`、`custom_wo_w2_w3_42.pt` 缺失或无法读取；`low_rank` schema 与预期不一致且不能确认如何安全裁剪；actual-bit 计算口径无法复核；tmux 目标 pane 无法唯一确认；GPU 长时间不可用；数据集下载或 HuggingFace 访问失败且现有缓存无法继续；需要修改量化核心代码；或连续三次同类运行失败。

默认选择理由：先做离线 `wqkv` rank ablation，因为它不用重跑重型 scale 生成，能最快验证“低敏感模块用更低 residual rank 降成本”的论文故事；swap 作为第二优先级，用来修复已有 Pareto 点。

Goal Draft (English-compatible)

/goal In `/home/users/Mayinyi/jikuixie/new-MBQ`, complete the 2026-06-30 No. 01 MBQ experiment batch: generate `wqkv` SVD rank 64 and 32 variants from existing caches offline, verify actual-bit cost and Wikitext2 PPL, run OCRBench for qualified variants, and if needed complete strict low-cost `51/42` swap repairs; write all valid results and evidence back to `MBQ_DAILY_PLAN.md`.
Verification: inspect the 2026-06-29 and 2026-06-30 OCRBench planning sections in `MBQ_DAILY_PLAN.md`, this `tmp.md` goal, `tools/build_custom_mbq_cache.py`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, `configs/internvl2/scripts/2_run_quant_eval.sh`, and `tests/test_PPL/2_compute_quant_ppl.sh`; confirm the `mixed_0.3` baseline is actual bit 2.3963 and OCRBench 701. Generate `wqkv_r64` and `wqkv_r32` cache variants for `custom_wo_all_w2_w3_55` and `custom_wo_w2_w3_42`; check each new cache for `attention.wqkv` low-rank count, `rank`, `up/down` shapes, `linear_bit_map`, `low_rank_count`, and `.summary.json`. Compute cost with `actual_bits = sum_i(numel(W_i) * bit_i) + sum_{i in low_rank}(numel(up_i) + numel(down_i)) * 4`, `actual bit = actual_bits / 6979321856`, and `Delta MiB vs mixed0.3 = (actual_bit - 2.3963) * 832.0`. Run Wikitext2 256-sample PPL for the 4 rank variants; run OCRBench for qualified `r64` variants, and run `r32` OCRBench only when the matching `r64` does not show clear OCRBench regression and the `r32` PPL is not catastrophic. If rank ablation fails or a low-cost repair is needed, construct and evaluate the highest-priority actual-bit-qualified candidates among `custom_51_swap_l4w2_l14w2`, `custom_51_swap_l10w2_l17w2`, `custom_51_swap_l11w2_l18w2`, `custom_42_swap_l19w2_l24w3`, and `custom_42_swap_l19w2_l23wo`. Record cache paths, summary paths, PPL logs, OCRBench outputs, actual bit, Delta MiB, OCRBench total and sub-scores, status, and conclusion in `MBQ_DAILY_PLAN.md`.
Constraints: do not rerun the full scale generation process for `wqkv` rank ablation; rank variants must be created by offline truncation of `attention.wqkv` low-rank factors from existing `custom_wo_all_w2_w3_55.pt` and `custom_wo_w2_w3_42.pt`, while all non-`wqkv` low-rank entries stay rank 128 and `linear_bit_map` plus 3-bit layer choices stay unchanged. Swap caches must be constructed from `scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_mixed_0.0.pt`, not by reverse-editing nonzero keep-ratio caches or existing custom caches. No candidate with actual bit greater than or equal to 2.3963 may be treated as a main Pareto candidate; do not use PPL as the primary metric instead of OCRBench; do not modify core quantization algorithms, model code, dataset logic, baseline results, unrelated experiment records, or non-current caches.
Boundaries: reading and writing are limited to `scale_cache/mbq`, `outputs`, `logs`, `MBQ_DAILY_PLAN.md`, `tmp.md`, `tools/build_custom_mbq_cache.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`. One focused offline cache processing script such as `tools/truncate_wqkv_low_rank.py` may be added, along with clearly named current-batch `.pt`, `.summary.json`, PPL logs, OCRBench outputs, and documentation records. Do not write or overwrite unrelated outputs, and do not edit `qmllm`, `main.py`, `main_quant.py`, third-party libraries, model weights, dataset source files, or unrelated configs.
Iteration policy: do discovery and consistency checks first, then process one candidate at a time. After generating each cache, check summary and actual bit before PPL; before every long task, query GPU status, and treat about 25 GiB free memory as enough for OCRBench rather than requiring a fully idle GPU. Prefer running long tasks through the tmux target pane: run `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'`, identify the pane with window_index 0 and pane_index 0, send commands with `tmux send-keys -t "$resolved_target" 'actual command' C-m`, and confirm start and finish using `tmux capture-pane` or log files. The recommended PPL command shape is `GPU_ID=available_gpu SCALE_FILE_NAME=candidate_file.pt DATASET=wikitext2 N_SAMPLES=256 bash tests/test_PPL/2_compute_quant_ppl.sh`. Before OCRBench, update `scale_path` and `output_path` in `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, update `LOG_FILE` and `GPU_ID` in `configs/internvl2/scripts/2_run_quant_eval.sh`, then read `outputs/my-internvl2-8b/candidate_name/results/ocrbench_results.txt` and the matching results json. On failure, inspect logs and summaries first; make at most 3 evidence-driven repair rounds, and pause after 3 repeated same-class failures.
Stop when: all 4 `wqkv` rank variants have been generated or explicitly reported unsafe to generate, and every generated variant has a summary, actual-bit verification, and Wikitext2 PPL record; qualified `r64` variants have OCRBench results, and qualified `r32` variants either have OCRBench results or a documented skip reason. If rank ablation does not produce a usable candidate, at least the top 2 strict low-cost swap candidates have cache construction, actual-bit verification, PPL screening, and required OCRBench evidence. `MBQ_DAILY_PLAN.md` must contain the 2026-06-30 result table, evidence paths, interim conclusions, whether any candidate replaces the `55/42/51` representatives, and next-step recommendations.
Pause if: new external credentials, paid resources, production data, destructive operations, or user approval to relax the OCRBench threshold is required; `mixed_0.0.pt`, `custom_wo_all_w2_w3_55.pt`, or `custom_wo_w2_w3_42.pt` is missing or unreadable; the `low_rank` schema differs from expectations and safe truncation is unclear; actual-bit accounting cannot be verified; the tmux target pane cannot be uniquely identified; GPUs remain unavailable; dataset download or HuggingFace access fails and existing cache cannot continue; core quantization code changes are required; or the same failure class repeats 3 times.

## 2026-06-30-02：wqkv rank 16 下界实验

推荐执行版（中文，可直接复制）

/goal 在 `/home/users/Mayinyi/jikuixie/new-MBQ` 中完成 2026-06-30 第 02 批 MBQ 实验：用现有 cache 离线生成 `custom_wo_w2_w3_42_wqkv_r16` 和必要时的 `custom_wo_all_w2_w3_55_wqkv_r16`，复核 actual-bit 成本、Wikitext2 256-sample PPL 和 OCRBench，判断 `attention.wqkv` low-rank residual 的可用下界是否可以从 rank 32 继续压到 rank 16，并把所有有效结果和证据写回 `MBQ_DAILY_PLAN.md`。
验证：先读取 `MBQ_DAILY_PLAN.md` 中 2026-06-30 的 `wqkv` rank ablation 执行记录和 rank 16 下界实验规划、`tmp.md` 本目标、`tools/truncate_wqkv_low_rank.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`；确认 `mixed_0.3` 基准为 actual bit 2.3963、OCRBench 701，并确认已有 `custom_wo_w2_w3_42_wqkv_r32` 为 actual bit 2.3622、OCRBench 695，已有 `custom_wo_all_w2_w3_55_wqkv_r64` 为 OCRBench 699。先用 `tools/truncate_wqkv_low_rank.py` 从 `custom_wo_w2_w3_42.pt` 生成 `custom_wo_w2_w3_42_wqkv_r16`，检查 `.summary.json` 中 `wqkv_low_rank_count=32`、`wqkv` rank 为 `{16:32}`、`up/down` shape 为 `(6144,16)/(16,4096)`、`linear_bit_map` 与 3-bit module counts 不变、actual bit 约为 2.3592 且低于 2.3963。对该候选运行 Wikitext2 256-sample PPL；若 PPL 未出现灾难性恶化，则运行 OCRBench。只有当 `custom_wo_w2_w3_42_wqkv_r16` 的 OCRBench 仍有分析价值时，再以相同方法从 `custom_wo_all_w2_w3_55.pt` 生成并评估 `custom_wo_all_w2_w3_55_wqkv_r16`。每个完成候选必须在 `MBQ_DAILY_PLAN.md` 记录 cache 路径、summary 路径、PPL json/log、OCRBench result txt/results json、actual bit、Delta MiB vs mixed0.3、Delta MiB vs r32、PPL、OCRBench 总分和子项、状态和结论。
约束：不要重新跑完整 scale 生成流程；r16 变体只能从已有原始母版 `custom_wo_w2_w3_42.pt` 和 `custom_wo_all_w2_w3_55.pt` 离线裁剪 `attention.wqkv` 的 `low_rank` 因子，不得从 r32 cache 反向修改；`wqkv` 之外的 low-rank 项必须保持 rank 128，`linear_bit_map`、3-bit 层选择、模型代码、量化核心算法、数据集逻辑和 baseline 结果不得改变。PPL 只作为灾难性退化快筛，OCRBench 仍是主指标；任何 actual bit 大于或等于 2.3963 的配置不得作为本轮主 Pareto 候选。本目标只做 `wqkv_r16` 下界实验，不启动严格 swap 搜索。
边界：允许读取和使用 `scale_cache/mbq`、`outputs`、`logs`、`MBQ_DAILY_PLAN.md`、`tmp.md`、`tools/truncate_wqkv_low_rank.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`。允许写入本轮明确命名的 r16 `.pt`、`.summary.json`、PPL 日志、OCRBench 输出和 `MBQ_DAILY_PLAN.md` 结果记录。禁止写入或覆盖非本轮实验输出，禁止改动 `qmllm`、`main.py`、`main_quant.py`、第三方库、模型权重、数据集源码和无关配置。
迭代策略：先做 discovery 和一致性检查，再一次只处理一个候选；优先顺序为 `custom_wo_w2_w3_42_wqkv_r16`，再根据结果决定是否运行 `custom_wo_all_w2_w3_55_wqkv_r16`。每次生成 cache 后先检查 summary、actual bit 和 rank shape，再进入 PPL；每次长任务前先查询 GPU，可用显存约 25 GiB 即可运行 OCRBench，不要求 GPU 完全空闲。长任务优先通过 tmux 目标 pane 执行：先运行 `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'` 找到 window_index 为 0 且 pane_index 为 0 的目标 pane，发送命令用 `tmux send-keys -t "$resolved_target" '实际命令' C-m`，随后用 `tmux capture-pane` 或日志文件确认启动和结束。PPL 推荐命令形态为 `GPU_ID=选定GPU编号 SCALE_FILE_NAME=候选文件名.pt DATASET=wikitext2 N_SAMPLES=256 bash tests/test_PPL/2_compute_quant_ppl.sh`。OCRBench 运行前更新 `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 的 `scale_path` 和 `output_path`，并更新 `configs/internvl2/scripts/2_run_quant_eval.sh` 的 `LOG_FILE` 和 `GPU_ID`；运行后读取 `outputs/my-internvl2-8b/候选名/results/ocrbench_results.txt` 和对应 results json。遇到失败先读日志和 summary，最多做 3 轮有新证据的修复；同类失败连续 3 次则暂停。
完成条件：`custom_wo_w2_w3_42_wqkv_r16` 已生成并完成 summary、actual-bit 复核、Wikitext2 PPL 和必要 OCRBench；如果其 OCRBench >= 690 或者 PPL 没有灾难性恶化但需要近无损线对照，则 `custom_wo_all_w2_w3_55_wqkv_r16` 也已完成同样记录。`MBQ_DAILY_PLAN.md` 中必须包含 2026-06-30-02 的结果表、证据路径、阶段性结论、是否替代 `42_r32` 或 `55_r64` 的判断，以及下一步建议。若 `42_r16` OCRBench 低于 690，则可停止并明确记录 rank 下界大概率在 16 和 32 之间。
暂停条件：需要新增外部凭证、付费资源、生产数据、破坏性操作或用户放宽 OCRBench 阈值；`custom_wo_w2_w3_42.pt`、`custom_wo_all_w2_w3_55.pt` 或 `mixed_0.0.pt` 缺失或无法读取；`low_rank` schema 与预期不一致且不能确认如何安全裁剪；actual-bit 计算口径无法复核；tmux 目标 pane 无法唯一确认；GPU 长时间不可用；数据集下载或 HuggingFace 访问失败且现有缓存无法继续；需要修改量化核心代码；或连续三次同类运行失败。

默认选择理由：先跑 `custom_wo_w2_w3_42_wqkv_r16`，因为它承接当前最有价值的激进低成本点 `42_r32=695`，能用最少 GPU 时间判断 r16 是否还有主结果价值；`55_r16` 只作为必要对照。

Goal Draft (English-compatible)

/goal In `/home/users/Mayinyi/jikuixie/new-MBQ`, complete the 2026-06-30 No. 02 MBQ experiment batch: generate `custom_wo_w2_w3_42_wqkv_r16` and, if justified, `custom_wo_all_w2_w3_55_wqkv_r16` from existing caches offline; verify actual-bit cost, Wikitext2 256-sample PPL, and OCRBench; determine whether `attention.wqkv` low-rank residual can be pushed from rank 32 down to rank 16; write all valid results and evidence back to `MBQ_DAILY_PLAN.md`.
Verification: inspect the 2026-06-30 `wqkv` rank ablation records and rank 16 planning section in `MBQ_DAILY_PLAN.md`, this `tmp.md` goal, `tools/truncate_wqkv_low_rank.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`; confirm the `mixed_0.3` baseline is actual bit 2.3963 and OCRBench 701, the existing `custom_wo_w2_w3_42_wqkv_r32` is actual bit 2.3622 with OCRBench 695, and the existing `custom_wo_all_w2_w3_55_wqkv_r64` has OCRBench 699. First generate `custom_wo_w2_w3_42_wqkv_r16` from `custom_wo_w2_w3_42.pt` using `tools/truncate_wqkv_low_rank.py`; check its `.summary.json` for `wqkv_low_rank_count=32`, `wqkv` rank `{16:32}`, `up/down` shape `(6144,16)/(16,4096)`, unchanged `linear_bit_map` and 3-bit module counts, actual bit around 2.3592 and below 2.3963. Run Wikitext2 256-sample PPL for that candidate; if PPL is not catastrophically worse, run OCRBench. Only if `custom_wo_w2_w3_42_wqkv_r16` remains analytically useful, generate and evaluate `custom_wo_all_w2_w3_55_wqkv_r16` from `custom_wo_all_w2_w3_55.pt` with the same checks. For every completed candidate, record cache path, summary path, PPL json/log, OCRBench result txt/results json, actual bit, Delta MiB vs mixed0.3, Delta MiB vs r32, PPL, OCRBench total and sub-scores, status, and conclusion in `MBQ_DAILY_PLAN.md`.
Constraints: do not rerun the full scale generation process; r16 variants must be created only by offline truncation of `attention.wqkv` low-rank factors from the original base caches `custom_wo_w2_w3_42.pt` and `custom_wo_all_w2_w3_55.pt`, not by reverse-editing r32 caches. All non-`wqkv` low-rank entries must stay rank 128, and `linear_bit_map`, 3-bit layer choices, model code, core quantization algorithms, dataset logic, and baseline results must remain unchanged. Use PPL only as a catastrophic-regression screen; OCRBench remains the primary metric. Do not treat any candidate with actual bit greater than or equal to 2.3963 as a main Pareto candidate. This goal is limited to `wqkv_r16` lower-bound testing and must not start strict swap search.
Boundaries: reading and writing are limited to `scale_cache/mbq`, `outputs`, `logs`, `MBQ_DAILY_PLAN.md`, `tmp.md`, `tools/truncate_wqkv_low_rank.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`. The agent may write clearly named current-batch r16 `.pt`, `.summary.json`, PPL logs, OCRBench outputs, and `MBQ_DAILY_PLAN.md` result records. Do not write or overwrite unrelated outputs, and do not edit `qmllm`, `main.py`, `main_quant.py`, third-party libraries, model weights, dataset source files, or unrelated configs.
Iteration policy: do discovery and consistency checks first, then process one candidate at a time. The priority order is `custom_wo_w2_w3_42_wqkv_r16` first, then decide whether to run `custom_wo_all_w2_w3_55_wqkv_r16` based on evidence. After generating each cache, check summary, actual bit, and rank shapes before PPL. Before every long task, query GPU status, and treat about 25 GiB free memory as enough for OCRBench rather than requiring a fully idle GPU. Prefer running long tasks through the tmux target pane: run `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'`, identify the pane with window_index 0 and pane_index 0, send commands with `tmux send-keys -t "$resolved_target" 'actual command' C-m`, and confirm start and finish using `tmux capture-pane` or log files. The recommended PPL command shape is `GPU_ID=selected_gpu SCALE_FILE_NAME=candidate_file.pt DATASET=wikitext2 N_SAMPLES=256 bash tests/test_PPL/2_compute_quant_ppl.sh`. Before OCRBench, update `scale_path` and `output_path` in `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, update `LOG_FILE` and `GPU_ID` in `configs/internvl2/scripts/2_run_quant_eval.sh`, then read `outputs/my-internvl2-8b/candidate_name/results/ocrbench_results.txt` and the matching results json. On failure, inspect logs and summaries first; make at most 3 evidence-driven repair rounds, and pause after 3 repeated same-class failures.
Stop when: `custom_wo_w2_w3_42_wqkv_r16` has cache generation, summary, actual-bit verification, Wikitext2 PPL, and required OCRBench evidence. If its OCRBench is at least 690, or if PPL is not catastrophic but a near-lossless-line comparison is needed, `custom_wo_all_w2_w3_55_wqkv_r16` also has the same records. `MBQ_DAILY_PLAN.md` must contain the 2026-06-30-02 result table, evidence paths, interim conclusions, whether either r16 candidate replaces `42_r32` or `55_r64`, and next-step recommendations. If `42_r16` OCRBench is below 690, stop and document that the usable rank lower bound is likely between 16 and 32.
Pause if: new external credentials, paid resources, production data, destructive operations, or user approval to relax the OCRBench threshold is required; `custom_wo_w2_w3_42.pt`, `custom_wo_all_w2_w3_55.pt`, or `mixed_0.0.pt` is missing or unreadable; the `low_rank` schema differs from expectations and safe truncation is unclear; actual-bit accounting cannot be verified; the tmux target pane cannot be uniquely identified; GPUs remain unavailable; dataset download or HuggingFace access fails and existing cache cannot continue; core quantization code changes are required; or the same failure class repeats 3 times.

## 2026-06-30-03：非 wqkv module residual rank sensitivity 实验

推荐执行版（中文，可直接复制）

/goal 在 `/home/users/Mayinyi/jikuixie/new-MBQ` 中完成 2026-06-30 第 03 批 MBQ 实验：以 `custom_wo_w2_w3_42_wqkv_r32` 为主母版，固定 `wqkv` rank 32 和现有 3-bit 选择，离线生成并评估非 `wqkv` module family 的单变量 residual rank 压缩候选，判断 `w1/w3/w2/wo` 哪些层族可以安全降低 rank，并把所有有效结果和证据写回 `MBQ_DAILY_PLAN.md`。
验证：先读取 `MBQ_DAILY_PLAN.md` 中 2026-06-30 第一批 `wqkv` rank ablation 执行记录、第二批 `wqkv_r16` 执行记录和第三批实验规划、`tmp.md` 本目标、`tools/truncate_wqkv_low_rank.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`；确认 `mixed_0.3` 基准为 actual bit 2.3963、OCRBench 701，确认 `custom_wo_w2_w3_42_wqkv_r32` 为 actual bit 2.3622、OCRBench 695、W2-256 PPL 24.4907，确认 `custom_wo_w2_w3_42_wqkv_r16` 为 OCRBench 689 且不作为后续母版。先泛化或新增一个离线 low-rank 裁剪脚本，使其支持 `--module w1/w2/w3/wo/wqkv` 和 `--rank`；用该脚本从 `custom_wo_w2_w3_42_wqkv_r32.pt` 生成 `custom_wo_w2_w3_42_wqkv_r32_w1_r64`、`custom_wo_w2_w3_42_wqkv_r32_w3_r64`、`custom_wo_w2_w3_42_wqkv_r32_w2_r64`、`custom_wo_w2_w3_42_wqkv_r32_wo_r32`。每个 cache 都必须检查 `.summary.json` 中 target module low-rank count、target rank、各 module rank 分布、`linear_bit_map` 与 3-bit module counts 不变、actual bit 和 Delta MiB；随后对 4 个候选运行 Wikitext2 256-sample PPL。OCRBench 默认先跑 `w1_r64`，再根据证据跑 `w3_r64` 或在 `w1_r64` 达到 OCRBench >= 693 时追加 `w1_r32`；`w2_r64` 和 `wo_r32` 只在 PPL 稳定且需要敏感层族或 attention residual 对照时进入 OCRBench。每个完成候选必须在 `MBQ_DAILY_PLAN.md` 记录 cache 路径、summary 路径、PPL json/log、OCRBench result txt/results json、actual bit、Delta MiB vs mixed0.3、Delta MiB vs base、target module、target rank、PPL、OCRBench 总分和子项、状态和结论。
约束：不要重新跑完整 scale 生成流程；本批候选只能从已有 `custom_wo_w2_w3_42_wqkv_r32.pt` 离线裁剪目标 module family 的 `low_rank` 因子，不能改变 `linear_bit_map`、3-bit 层选择、非目标 module rank、模型代码、量化核心算法、数据集逻辑、baseline 结果或已有无关实验记录。PPL 只作为灾难性退化快筛，OCRBench 仍是主指标；不得把多个 module family 一开始同时降 rank，组合实验只能在单 module OCRBench 证据明确后进行。任何 actual bit 大于或等于 2.3963 的配置不得作为本轮主 Pareto 候选。
边界：允许读取和使用 `scale_cache/mbq`、`outputs`、`logs`、`MBQ_DAILY_PLAN.md`、`tmp.md`、`tools/truncate_wqkv_low_rank.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`。允许新增或修改一个聚焦的离线 cache 处理脚本，例如 `tools/truncate_low_rank_by_module.py`，以及为本轮候选写入明确命名的 `.pt`、`.summary.json`、PPL 日志、OCRBench 输出和文档记录。禁止写入或覆盖非本轮实验输出，禁止改动 `qmllm`、`main.py`、`main_quant.py`、第三方库、模型权重、数据集源码和无关配置。
迭代策略：先做 discovery 和一致性检查，再一次只处理一个候选。每次生成 cache 后先检查 summary、actual bit、target module rank 和非目标 module rank，再进入 PPL；PPL 完成后按“存储收益优先、PPL 不灾难、OCRBench 成本受控”的顺序选择最多 2 个候选进入 OCRBench。长任务前先查询 GPU，可用显存约 25 GiB 即可运行 OCRBench，不要求 GPU 完全空闲。长任务优先通过 tmux 目标 pane 执行：先运行 `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'` 找到 window_index 为 0 且 pane_index 为 0 的目标 pane，发送命令用 `tmux send-keys -t "$resolved_target" '实际命令' C-m`，随后用 `tmux capture-pane` 或日志文件确认启动和结束。PPL 推荐命令形态为 `GPU_ID=选定GPU编号 SCALE_FILE_NAME=候选文件名.pt DATASET=wikitext2 N_SAMPLES=256 bash tests/test_PPL/2_compute_quant_ppl.sh`。OCRBench 运行前更新 `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 的 `scale_path` 和 `output_path`，并更新 `configs/internvl2/scripts/2_run_quant_eval.sh` 的 `LOG_FILE` 和 `GPU_ID`；运行后读取 `outputs/my-internvl2-8b/候选名/results/ocrbench_results.txt` 和对应 results json。遇到失败先读日志和 summary，最多做 3 轮有新证据的修复；同类失败连续 3 次则暂停。
完成条件：4 个单 module 候选都已生成或明确说明无法安全生成，并且每个已生成候选都有 summary、actual-bit 复核和 Wikitext2 PPL 记录；`w1_r64` 已按门槛完成 OCRBench 或有明确跳过理由，且至少另一个高价值候选完成 OCRBench 或有基于 PPL、成本和前序 OCRBench 的明确跳过理由。如果 `w1_r64` OCRBench >= 693，则 `w1_r32` 已完成 cache 构造、summary、PPL 和必要 OCRBench，或明确记录暂不追加的理由。`MBQ_DAILY_PLAN.md` 中必须包含 2026-06-30-03 结果表、证据路径、阶段性结论、哪些 module family 可继续压 rank、是否替代 `custom_wo_w2_w3_42_wqkv_r32` 的判断，以及下一步建议。
暂停条件：需要新增外部凭证、付费资源、生产数据、破坏性操作或用户放宽 OCRBench 阈值；`custom_wo_w2_w3_42_wqkv_r32.pt`、`mixed_0.0.pt` 或参考 summary 缺失或无法读取；`low_rank` schema 与预期不一致且不能确认如何安全裁剪；actual-bit 计算口径无法复核；tmux 目标 pane 无法唯一确认；GPU 长时间不可用；数据集下载或 HuggingFace 访问失败且现有缓存无法继续；需要修改量化核心代码；或连续三次同类运行失败。

默认选择理由：先做单 module rank sensitivity screen，因为它能用最少 OCRBench 预算判断 `w1/w3/w2/wo` 的 residual rank 敏感性，并避免组合压缩失败后无法归因。

Goal Draft (English-compatible)

/goal In `/home/users/Mayinyi/jikuixie/new-MBQ`, complete the 2026-06-30 No. 03 MBQ experiment batch: use `custom_wo_w2_w3_42_wqkv_r32` as the main base, keep `wqkv` at rank 32 and keep the current 3-bit selections unchanged, generate and evaluate single-module-family non-`wqkv` residual rank compression candidates offline, determine which of `w1/w3/w2/wo` can safely use lower residual rank, and write all valid results and evidence back to `MBQ_DAILY_PLAN.md`.
Verification: inspect the 2026-06-30 first-batch `wqkv` rank ablation records, the second-batch `wqkv_r16` execution records, the third-batch plan in `MBQ_DAILY_PLAN.md`, this `tmp.md` goal, `tools/truncate_wqkv_low_rank.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`; confirm the `mixed_0.3` baseline is actual bit 2.3963 with OCRBench 701, confirm `custom_wo_w2_w3_42_wqkv_r32` has actual bit 2.3622, OCRBench 695, and W2-256 PPL 24.4907, and confirm `custom_wo_w2_w3_42_wqkv_r16` has OCRBench 689 and is not a base for further work. First generalize or add an offline low-rank truncation script that supports `--module w1/w2/w3/wo/wqkv` and `--rank`; use it to generate `custom_wo_w2_w3_42_wqkv_r32_w1_r64`, `custom_wo_w2_w3_42_wqkv_r32_w3_r64`, `custom_wo_w2_w3_42_wqkv_r32_w2_r64`, and `custom_wo_w2_w3_42_wqkv_r32_wo_r32` from `custom_wo_w2_w3_42_wqkv_r32.pt`. For each cache, check the `.summary.json` for target module low-rank count, target rank, rank distribution by module, unchanged `linear_bit_map` and 3-bit module counts, actual bit, and Delta MiB; then run Wikitext2 256-sample PPL for all 4 candidates. Run OCRBench for `w1_r64` first by default, then run `w3_r64` or, if `w1_r64` reaches OCRBench >= 693, add `w1_r32`; only run `w2_r64` and `wo_r32` OCRBench when PPL is stable and a sensitive-module or attention-residual contrast is needed. For every completed candidate, record cache path, summary path, PPL json/log, OCRBench result txt/results json, actual bit, Delta MiB vs mixed0.3, Delta MiB vs base, target module, target rank, PPL, OCRBench total and sub-scores, status, and conclusion in `MBQ_DAILY_PLAN.md`.
Constraints: do not rerun the full scale generation process; current-batch candidates must be created only by offline truncation of the target module family's `low_rank` factors from `custom_wo_w2_w3_42_wqkv_r32.pt`, without changing `linear_bit_map`, 3-bit layer choices, non-target module ranks, model code, core quantization algorithms, dataset logic, baseline results, or unrelated experiment records. Use PPL only as a catastrophic-regression screen; OCRBench remains the primary metric. Do not lower multiple module families at once at the start; combination experiments are allowed only after single-module OCRBench evidence is clear. Do not treat any candidate with actual bit greater than or equal to 2.3963 as a main Pareto candidate.
Boundaries: reading and writing are limited to `scale_cache/mbq`, `outputs`, `logs`, `MBQ_DAILY_PLAN.md`, `tmp.md`, `tools/truncate_wqkv_low_rank.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`. The agent may add or modify one focused offline cache-processing script such as `tools/truncate_low_rank_by_module.py`, and may write clearly named current-batch `.pt`, `.summary.json`, PPL logs, OCRBench outputs, and documentation records. Do not write or overwrite unrelated outputs, and do not edit `qmllm`, `main.py`, `main_quant.py`, third-party libraries, model weights, dataset source files, or unrelated configs.
Iteration policy: do discovery and consistency checks first, then process one candidate at a time. After generating each cache, check summary, actual bit, target module rank, and non-target module ranks before PPL; after PPL, choose at most 2 candidates for OCRBench by prioritizing storage savings, non-catastrophic PPL, and controlled OCRBench cost. Before every long task, query GPU status, and treat about 25 GiB free memory as enough for OCRBench rather than requiring a fully idle GPU. Prefer running long tasks through the tmux target pane: run `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'`, identify the pane with window_index 0 and pane_index 0, send commands with `tmux send-keys -t "$resolved_target" 'actual command' C-m`, and confirm start and finish using `tmux capture-pane` or log files. The recommended PPL command shape is `GPU_ID=selected_gpu SCALE_FILE_NAME=candidate_file.pt DATASET=wikitext2 N_SAMPLES=256 bash tests/test_PPL/2_compute_quant_ppl.sh`. Before OCRBench, update `scale_path` and `output_path` in `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, update `LOG_FILE` and `GPU_ID` in `configs/internvl2/scripts/2_run_quant_eval.sh`, then read `outputs/my-internvl2-8b/candidate_name/results/ocrbench_results.txt` and the matching results json. On failure, inspect logs and summaries first; make at most 3 evidence-driven repair rounds, and pause after 3 repeated same-class failures.
Stop when: all 4 single-module candidates have been generated or explicitly reported unsafe to generate, and every generated candidate has a summary, actual-bit verification, and Wikitext2 PPL record; `w1_r64` has OCRBench evidence or a documented skip reason, and at least one additional high-value candidate has OCRBench evidence or a clear skip reason based on PPL, cost, and prior OCRBench evidence. If `w1_r64` reaches OCRBench >= 693, `w1_r32` has cache construction, summary, PPL, and required OCRBench evidence, or a documented reason not to add it yet. `MBQ_DAILY_PLAN.md` must contain the 2026-06-30-03 result table, evidence paths, interim conclusions, which module families can keep lower rank, whether any candidate replaces `custom_wo_w2_w3_42_wqkv_r32`, and next-step recommendations.
Pause if: new external credentials, paid resources, production data, destructive operations, or user approval to relax the OCRBench threshold is required; `custom_wo_w2_w3_42_wqkv_r32.pt`, `mixed_0.0.pt`, or reference summaries are missing or unreadable; the `low_rank` schema differs from expectations and safe truncation is unclear; actual-bit accounting cannot be verified; the tmux target pane cannot be uniquely identified; GPUs remain unavailable; dataset download or HuggingFace access fails and existing cache cannot continue; core quantization code changes are required; or the same failure class repeats 3 times.

## 2026-06-30-04：组合 residual rank 下界密集实验

推荐执行版（中文，可直接复制）

/goal 在 `/home/users/Mayinyi/jikuixie/new-MBQ` 中完成 2026-06-30 第 04 批 MBQ 实验：基于已验证的 `wqkv` rank 压缩、`w3_r64`、`w1_r64` 边界、少量 `wo/w2` 对照，尽量跑满组合 residual rank 压缩矩阵，生成 55 线和 42 线的组合 cache，完成 actual-bit、Wikitext2 256-sample PPL 和 OCRBench，明确组合压缩的可用区间与下限，并把所有结果和证据写回 `MBQ_DAILY_PLAN.md`。
验证：先读取 `MBQ_DAILY_PLAN.md` 中 2026-06-30 第一批 `wqkv` rank ablation、第二批 `wqkv_r16`、第三批非 `wqkv` module rank sensitivity 执行记录，以及本 `tmp.md` 目标；确认基准 `mixed_0.3` 为 actual bit 2.3963、OCRBench 701；确认已完成点包括 `custom_wo_all_w2_w3_55_wqkv_r64` OCRBench 699、`custom_wo_all_w2_w3_55_wqkv_r16` OCRBench 695、`custom_wo_w2_w3_42_wqkv_r32` OCRBench 695、`custom_wo_w2_w3_42_wqkv_r32_w3_r64` OCRBench 693、`custom_wo_w2_w3_42_wqkv_r32_w1_r64` OCRBench 692。检查 `tools/truncate_low_rank_by_module.py`、`tools/truncate_wqkv_low_rank.py`、`tests/test_truncate_low_rank_by_module.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`。若现有 `tools/truncate_low_rank_by_module.py` 只能一次处理一个 module，则扩展或新增一个聚焦脚本，使其支持多 module/rank 组合，例如 `--set w1:64 --set w3:64 --set wo:32`，并增加最小单元测试。为以下候选尽量全部生成 cache 和 `.summary.json`：`custom_wo_all_w2_w3_55_wqkv_r16_w3_r64`、`custom_wo_all_w2_w3_55_wqkv_r64_w3_r64`、`custom_wo_all_w2_w3_55_wqkv_r32_w3_r64`、`custom_wo_all_w2_w3_55_wqkv_r64_w1_r64`、`custom_wo_all_w2_w3_55_wqkv_r16_w1_r64`、`custom_wo_all_w2_w3_55_wqkv_r64_w1_r64_w3_r64`、`custom_wo_all_w2_w3_55_wqkv_r32_w1_r64_w3_r64`、`custom_wo_all_w2_w3_55_wqkv_r16_w1_r64_w3_r64`、`custom_wo_w2_w3_42_wqkv_r32_w3_r32`、`custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64`、`custom_wo_w2_w3_42_wqkv_r32_w3_r64_wo_r32`、`custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r32`、`custom_wo_all_w2_w3_55_wqkv_r64_w3_r32`、`custom_wo_all_w2_w3_55_wqkv_r16_w3_r32`、`custom_wo_all_w2_w3_55_wqkv_r64_w2_r64`、`custom_wo_w2_w3_42_wqkv_r32_w3_r64_w2_r64`。每个 cache 必须复核 target module/rank set、各 module rank 分布、`linear_bit_map` 与 3-bit module counts 不变、actual bit、Delta MiB vs `mixed_0.3`、Delta MiB vs immediate base、Delta MiB vs root base。所有候选都运行 Wikitext2 256-sample PPL；PPL 很差、异常大、NaN 或相对母版明显退化，都只作为风险标签记录，不能作为跳过 OCRBench 或停止后续候选的理由。OCRBench 按 `55_wqkv_r16_w3_r64`、`55_wqkv_r64_w3_r64`、`55_wqkv_r32_w3_r64`、`55_wqkv_r64_w1_r64`、`55_wqkv_r16_w1_r64`、`55_wqkv_r64_w1_r64_w3_r64`、`55_wqkv_r32_w1_r64_w3_r64`、`55_wqkv_r16_w1_r64_w3_r64`、`42_wqkv_r32_w3_r32`、`42_wqkv_r32_w1_r64_w3_r64`、`42_wqkv_r32_w3_r64_wo_r32`、`42_wqkv_r32_w1_r64_w3_r64_wo_r32`、`55_wqkv_r64_w3_r32`、`55_wqkv_r16_w3_r32`、`55_wqkv_r64_w2_r64`、`42_wqkv_r32_w3_r64_w2_r64` 的顺序尽量跑满。每个完成候选必须在 `MBQ_DAILY_PLAN.md` 记录 cache 路径、summary 路径、PPL json/log 或 PPL 失败标签、OCRBench result txt/results json、actual bit、Delta MiB、rank distribution、OCRBench 总分和子项、status 和结论。
约束：不要重新跑完整 scale search；组合候选只能从已有安全 cache 或本批生成的中间 cache 离线裁剪 low-rank 因子，不改变 `linear_bit_map`、3-bit 层选择、非目标 module rank、模型代码、量化核心算法、数据集逻辑、baseline 结果或已有无关实验记录。PPL 不是筛选门槛，OCRBench 低分也不是停止条件；本批重点是测压缩下限和失败边界。55 线默认不做 `wo_r32`，因为 `custom_wo_all_w2_w3_55` 已经把 32 个 `attention.wo` 全部升到 3-bit；除非 summary 显示仍有 `wo` residual，否则不要强行生成无效 `wo` 组合。任何 actual bit 大于或等于 2.3963 的配置不得作为主 Pareto 候选，但仍可作为反例记录。
边界：允许读取和使用 `scale_cache/mbq`、`outputs`、`logs`、`MBQ_DAILY_PLAN.md`、`tmp.md`、`tools/truncate_low_rank_by_module.py`、`tools/truncate_wqkv_low_rank.py`、`tests/test_truncate_low_rank_by_module.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`、`configs/internvl2/scripts/2_run_quant_eval.sh`。允许修改或新增聚焦的离线 cache 处理脚本和单元测试，允许写入本批明确命名的 `.pt`、`.summary.json`、PPL 日志、OCRBench 输出和 `MBQ_DAILY_PLAN.md` 结果记录。禁止写入或覆盖非本轮实验输出，禁止改动 `qmllm`、`main.py`、`main_quant.py`、第三方库、模型权重、数据集源码和无关配置。
迭代策略：先做 discovery 和一致性检查，再批量生成 cache；每个 cache 生成后先检查 summary、actual bit、target module rank 和非目标 module rank。然后串行或按 GPU 安全情况运行 PPL；PPL 出现异常时先判断是实验性退化还是基础设施失败，若是基础设施失败则换 GPU/重跑，若是实验性退化则记录标签并继续 OCRBench。OCRBench 长任务优先通过 tmux 目标 pane 执行：先运行 `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'` 找到 window_index 为 0 且 pane_index 为 0 的目标 pane，发送命令用 `tmux send-keys -t "$resolved_target" '实际命令' C-m`，随后用 `tmux capture-pane` 或日志文件确认启动和结束。每次 OCRBench 前更新 `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 的 `scale_path` 和 `output_path`，并更新 `configs/internvl2/scripts/2_run_quant_eval.sh` 的 `LOG_FILE` 和 `GPU_ID`。每个候选结束后立即把路径、分数、失败标签和结论追加到 `MBQ_DAILY_PLAN.md`，避免长任务中断后丢失上下文。
完成条件：16 个 planned candidates 都已生成或明确说明无法安全生成；每个已生成候选都有 summary、actual-bit 复核、PPL 结果或 PPL 失败标签、OCRBench 结果或明确基础设施阻塞记录。`MBQ_DAILY_PLAN.md` 中必须包含 2026-06-30-04 结果表、证据路径、PPL 崩坏标签、OCRBench 总分和子项、哪些组合仍可用、哪些组合越过下界、是否产生可替代 `55_wqkv_r64`、`55_wqkv_r16`、`42_wqkv_r32` 或 `42_wqkv_r32_w3_r64` 的新代表点，以及下一步建议。
暂停条件：需要新增外部凭证、付费资源、生产数据、破坏性操作；本批必要母版 cache 缺失或无法读取；`low_rank` schema 与预期不一致且不能确认如何安全裁剪；actual-bit 计算口径无法复核；tmux 目标 pane 无法唯一确认；GPU 长时间不可用；数据集下载或 HuggingFace 访问失败且现有缓存无法继续；需要修改量化核心代码；或同一基础设施失败连续三次且没有新的修复证据。不要因为 PPL 崩、PPL 极差、OCRBench 低分或候选低于 690 而暂停。

默认选择理由：本批目标是尽量跑满组合矩阵来观察压缩下限，因此把 PPL 和低 OCRBench 都视为有价值证据，而不是提前停止条件。

Goal Draft (English-compatible)

/goal In `/home/users/Mayinyi/jikuixie/new-MBQ`, complete the 2026-06-30 No. 04 MBQ experiment batch: build and evaluate a dense combination matrix of residual-rank compression candidates using the validated `wqkv` rank reductions, `w3_r64`, borderline `w1_r64`, and limited `wo/w2` contrast points; run actual-bit checks, Wikitext2 256-sample PPL, and OCRBench as fully as possible; determine the usable region and lower bound of combined residual-rank compression; write all results and evidence back to `MBQ_DAILY_PLAN.md`.
Verification: inspect the 2026-06-30 first-batch `wqkv` rank ablation records, second-batch `wqkv_r16` records, third-batch non-`wqkv` module rank sensitivity records in `MBQ_DAILY_PLAN.md`, and this `tmp.md` goal. Confirm `mixed_0.3` has actual bit 2.3963 and OCRBench 701; confirm completed points including `custom_wo_all_w2_w3_55_wqkv_r64` OCRBench 699, `custom_wo_all_w2_w3_55_wqkv_r16` OCRBench 695, `custom_wo_w2_w3_42_wqkv_r32` OCRBench 695, `custom_wo_w2_w3_42_wqkv_r32_w3_r64` OCRBench 693, and `custom_wo_w2_w3_42_wqkv_r32_w1_r64` OCRBench 692. Inspect `tools/truncate_low_rank_by_module.py`, `tools/truncate_wqkv_low_rank.py`, `tests/test_truncate_low_rank_by_module.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`. If the current low-rank truncation script only supports one module at a time, extend it or add a focused helper supporting multi-module/rank specs such as `--set w1:64 --set w3:64 --set wo:32`, with a minimal unit test. Generate cache and `.summary.json` files for these candidates as fully as possible: `custom_wo_all_w2_w3_55_wqkv_r16_w3_r64`, `custom_wo_all_w2_w3_55_wqkv_r64_w3_r64`, `custom_wo_all_w2_w3_55_wqkv_r32_w3_r64`, `custom_wo_all_w2_w3_55_wqkv_r64_w1_r64`, `custom_wo_all_w2_w3_55_wqkv_r16_w1_r64`, `custom_wo_all_w2_w3_55_wqkv_r64_w1_r64_w3_r64`, `custom_wo_all_w2_w3_55_wqkv_r32_w1_r64_w3_r64`, `custom_wo_all_w2_w3_55_wqkv_r16_w1_r64_w3_r64`, `custom_wo_w2_w3_42_wqkv_r32_w3_r32`, `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64`, `custom_wo_w2_w3_42_wqkv_r32_w3_r64_wo_r32`, `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r32`, `custom_wo_all_w2_w3_55_wqkv_r64_w3_r32`, `custom_wo_all_w2_w3_55_wqkv_r16_w3_r32`, `custom_wo_all_w2_w3_55_wqkv_r64_w2_r64`, and `custom_wo_w2_w3_42_wqkv_r32_w3_r64_w2_r64`. For every cache, verify the target module/rank set, rank distribution by module, unchanged `linear_bit_map` and 3-bit module counts, actual bit, Delta MiB vs `mixed_0.3`, Delta MiB vs immediate base, and Delta MiB vs root base. Run Wikitext2 256-sample PPL for all candidates; bad PPL, extremely high PPL, NaN PPL, or strong PPL regression must be recorded only as risk labels and must not be used to skip OCRBench or stop later candidates. Run OCRBench in this order as fully as possible: `55_wqkv_r16_w3_r64`, `55_wqkv_r64_w3_r64`, `55_wqkv_r32_w3_r64`, `55_wqkv_r64_w1_r64`, `55_wqkv_r16_w1_r64`, `55_wqkv_r64_w1_r64_w3_r64`, `55_wqkv_r32_w1_r64_w3_r64`, `55_wqkv_r16_w1_r64_w3_r64`, `42_wqkv_r32_w3_r32`, `42_wqkv_r32_w1_r64_w3_r64`, `42_wqkv_r32_w3_r64_wo_r32`, `42_wqkv_r32_w1_r64_w3_r64_wo_r32`, `55_wqkv_r64_w3_r32`, `55_wqkv_r16_w3_r32`, `55_wqkv_r64_w2_r64`, and `42_wqkv_r32_w3_r64_w2_r64`. For every completed candidate, record cache path, summary path, PPL json/log or PPL failure label, OCRBench result txt/results json, actual bit, Delta MiB, rank distribution, OCRBench total and sub-scores, status, and conclusion in `MBQ_DAILY_PLAN.md`.
Constraints: do not rerun full scale search; combination candidates must be built only by offline truncation from existing safe caches or current-batch intermediate caches, without changing `linear_bit_map`, 3-bit selections, non-target module ranks, model code, core quantization algorithms, dataset logic, baseline results, or unrelated experiment records. PPL is not a screening gate, and low OCRBench is not a stop condition; this batch is specifically for lower-bound and failure-boundary evidence. Do not build `wo_r32` combinations on the 55 line unless the summary shows remaining `wo` low-rank residuals, because `custom_wo_all_w2_w3_55` should already have all 32 `attention.wo` layers at 3-bit. Candidates with actual bit greater than or equal to 2.3963 must not be treated as main Pareto candidates, but may still be recorded as counterexamples.
Boundaries: read/write scope is limited to `scale_cache/mbq`, `outputs`, `logs`, `MBQ_DAILY_PLAN.md`, `tmp.md`, `tools/truncate_low_rank_by_module.py`, `tools/truncate_wqkv_low_rank.py`, `tests/test_truncate_low_rank_by_module.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`. A focused offline cache-processing helper and unit test may be modified or added, and clearly named current-batch `.pt`, `.summary.json`, PPL logs, OCRBench outputs, and documentation records may be written. Do not write or overwrite unrelated outputs, and do not edit `qmllm`, `main.py`, `main_quant.py`, third-party libraries, model weights, dataset source files, or unrelated configs.
Iteration policy: do discovery and consistency checks first, then generate caches in batch. After each cache is generated, verify summary, actual bit, target module rank, and non-target module ranks. Then run PPL serially or with safe GPU scheduling; if PPL fails, classify whether it is an infrastructure failure or an experimental degradation. Infrastructure failures should be retried on another GPU or with a clean run; experimental degradation should be recorded as a label and OCRBench should still run. Prefer OCRBench through the tmux target pane: identify the pane with `tmux list-panes -a -F '#{session_name}:#{window_index}.#{pane_index} #{pane_current_path} #{pane_title}'`, send commands with `tmux send-keys -t "$resolved_target" 'actual command' C-m`, and confirm start/finish through `tmux capture-pane` or logs. Before each OCRBench run, update `scale_path` and `output_path` in `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `LOG_FILE` plus `GPU_ID` in `configs/internvl2/scripts/2_run_quant_eval.sh`. After every candidate finishes, immediately append paths, scores, failure labels, and conclusions to `MBQ_DAILY_PLAN.md`.
Stop when: all 16 planned candidates have been generated or explicitly reported unsafe to generate; every generated candidate has summary and actual-bit verification, a PPL result or PPL failure label, and an OCRBench result or clearly documented infrastructure blocker. `MBQ_DAILY_PLAN.md` must contain the 2026-06-30-04 result table, evidence paths, PPL failure labels, OCRBench total and sub-scores, which combinations remain usable, which combinations cross the lower bound, whether any new candidate replaces `55_wqkv_r64`, `55_wqkv_r16`, `42_wqkv_r32`, or `42_wqkv_r32_w3_r64`, and next-step recommendations.
Pause if: new external credentials, paid resources, production data, or destructive operations are required; required base caches are missing or unreadable; the `low_rank` schema differs from expectations and safe truncation is unclear; actual-bit accounting cannot be verified; the tmux target pane cannot be uniquely identified; GPUs remain unavailable; dataset download or HuggingFace access fails and existing cache cannot continue; core quantization code changes are required; or the same infrastructure failure repeats three times without new repair evidence. Do not pause because PPL collapses, PPL is very poor, OCRBench is low, or a candidate scores below 690.

## 2026-07-06-01：当前锚点的 `wo_r48` 与 `w2_r96` 局部梯队实验

推荐执行版（中文，可直接复制）

/goal 在 `/home/users/Mayinyi/jikuixie/new-MBQ` 中完成 2026-07-06 第 01 批 MBQ 实验：以当前锚点 `wqkv:32, w1:64, w3:64, wo:32, w2:128` 为中心，从上游 `custom_wo_w2_w3_42_wqkv_r32` cache 重新裁剪生成 `wo_r48`、`wo_r32_w2_r96`、必要的 `wo_r48_w2_r96` 和可选 `wo_r64` 局部梯队，完成 actual-bit、Wikitext2 256-sample PPL 和 OCRBench，判断 `wo_r32` 是否过激以及 `w2_r96` 是否还能加入激进低成本组合，并把所有有效结果和证据写回 `MBQ_DAILY_PLAN.md`。
验证：先读取 `MBQ_DAILY_PLAN.md` 中 2026-07-06 实验规划、本 `tmp.md` 目标、`tools/truncate_low_rank_by_module.py`、`tests/test_truncate_low_rank_by_module.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 和 `configs/internvl2/scripts/2_run_quant_eval.sh`；确认基准 `mixed_0.3` 为 actual bit 2.3963、OCRBench 701，确认当前锚点 `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r32` 为 actual bit 2.3176、Delta MiB vs mixed_0.3 为 -65.47 MiB、OCRBench 683，并确认对照 `42_wqkv_r32_w1_r64_w3_r64` 为 OCRBench 692、`42_wqkv_r32_w3_r64_wo_r32` 为 OCRBench 692。必须从 `scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_custom_wo_w2_w3_42_wqkv_r32.pt` 生成候选，不得从已经截断到 `wo_r32` 的 cache 恢复 `wo_r48/wo_r64`。按顺序生成并检查以下候选：`custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r48`，语义配置 `wqkv:32,w1:64,w3:64,wo:48,w2:128`；`custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r32_w2_r96`，语义配置 `wqkv:32,w1:64,w3:64,wo:32,w2:96`；若 `wo_r48` 对 683 有明显修复，再生成 `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r48_w2_r96`，语义配置 `wqkv:32,w1:64,w3:64,wo:48,w2:96`；只有当 `wo_r48` 仍低但明显高于 `wo_r32` 时，才补 `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r64`，语义配置 `wqkv:32,w1:64,w3:64,wo:64,w2:128`。每个生成的 cache 都必须复核 `.summary.json` 中的 `target_rank_set`、`low_rank_ranks_by_module`、`linear_bit_map` 不变、3-bit module counts 不变、`low_rank_count`、actual bit 和 Delta MiB vs `mixed_0.3`。每个主候选都运行 Wikitext2 256-sample PPL；PPL 只作为风险标签，不作为跳过 OCRBench 的硬门槛。OCRBench 至少覆盖前三个主候选，`wo_r64` 只在触发条件满足时评测。每个完成候选必须在 `MBQ_DAILY_PLAN.md` 记录 cache 路径、summary 路径、PPL json/log、OCRBench result txt/results json、actual bit、Delta MiB、rank distribution、3-bit module counts、OCRBench 总分和子项、状态和结论。
约束：不要重新跑完整 scale search；不要从 `wo_r32` cache 生成 `wo_r48/wo_r64`，因为 rank32 tensor 无法恢复 rank48/64；候选只能从上游 `custom_wo_w2_w3_42_wqkv_r32.pt` 离线裁剪 low-rank 因子，不改变 `linear_bit_map`、3-bit 层选择、非目标 module rank、模型代码、量化核心算法、数据集逻辑、baseline 结果或已有无关实验记录。PPL 不是主指标，最终判断以 OCRBench 为主。任何 actual bit 大于或等于 2.3963 的配置不得作为主 Pareto 候选。若默认 `python3` 环境缺少 `torch`，使用项目可用的 `mbq`/conda Python 环境运行 cache 生成与测试，而不是改动脚本逻辑。
边界：允许读取和使用 `scale_cache/mbq`、`outputs`、`logs`、`MBQ_DAILY_PLAN.md`、`tmp.md`、`tools/truncate_low_rank_by_module.py`、`tests/test_truncate_low_rank_by_module.py`、`tests/test_PPL/2_compute_quant_ppl.sh`、`configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 和 `configs/internvl2/scripts/2_run_quant_eval.sh`。允许写入本批明确命名的 `.pt`、`.summary.json`、PPL 日志、OCRBench 输出和 `MBQ_DAILY_PLAN.md` 结果记录；必要时可对 `my_eval_ocrbench_svd.yaml` 和 `2_run_quant_eval.sh` 做本轮运行所需的 scale/output/log/GPU 配置切换。禁止写入或覆盖非本轮实验输出，禁止改动 `qmllm`、`main.py`、`main_quant.py`、第三方库、模型权重、数据集源码和无关配置。
迭代策略：先做 discovery 和一致性检查，再一次只处理一个候选。生成命令形态为 `python3 tools/truncate_low_rank_by_module.py --base-cache scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_custom_wo_w2_w3_42_wqkv_r32.pt --set w1:64 --set w3:64 --set wo:48 --name custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r48`，`wo_r32_w2_r96` 增加 `--set wo:32 --set w2:96`，`wo_r48_w2_r96` 使用 `--set wo:48 --set w2:96`，可选 `wo_r64` 使用 `--set wo:64`。每次生成 cache 后先检查 summary、actual bit、target module rank 和非目标 module rank，再进入 PPL。长任务前先查询 GPU：`nvidia-smi --query-gpu=index,name,memory.free,memory.used,utilization.gpu --format=csv`；PPL 推荐命令形态为 `GPU_ID=选定GPU编号 DATASET=wikitext2 N_SAMPLES=256 SCALE_FILE_NAME=候选文件名.pt bash tests/test_PPL/2_compute_quant_ppl.sh`。OCRBench 运行前更新 `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml` 的 `scale_path` 和 `output_path`，并更新 `configs/internvl2/scripts/2_run_quant_eval.sh` 的 `LOG_FILE` 和 `GPU_ID`；运行后读取 `outputs/my-internvl2-8b/候选名/results/ocrbench_results.txt` 和对应 results json。遇到失败先读日志和 summary，最多做 3 轮有新证据的修复；同类基础设施失败连续 3 次则暂停。
完成条件：三个主候选 `wo_r48`、`wo_r32_w2_r96`、`wo_r48_w2_r96` 已生成或明确说明不满足触发条件/无法安全生成，并且每个已生成候选都有 summary、actual-bit 复核、Wikitext2 PPL 和 OCRBench 结果；如果 `wo_r48` 触发 `wo_r64` 复核条件，则 `wo_r64` 也完成相同记录或有明确阻塞说明。`MBQ_DAILY_PLAN.md` 中必须包含 2026-07-06-01 结果表、证据路径、PPL 风险标签、OCRBench 总分和子项，以及对四个问题的结论：当前锚点 683 是否主要由 `wo_r32` 造成，`wo` 的安全 rank 是否在 48 或 64 附近，`w2_r96` 是否可继续压，最终更合理的激进组合是否应写成 `wqkv:32 + w1:64 + w3:64 + wo:48 + optional w2:96`。
暂停条件：需要新增外部凭证、付费资源、生产数据、破坏性操作或用户改变评测阈值；上游 base cache `scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_custom_wo_w2_w3_42_wqkv_r32.pt` 缺失或无法读取；`low_rank` schema 与预期不一致且不能确认如何安全裁剪；actual-bit 计算口径无法复核；tmux/GPU 运行目标无法确认且长任务无法安全启动；数据集下载或 HuggingFace 访问失败且现有缓存无法继续；需要修改量化核心代码；或同一基础设施失败连续三次且没有新的修复证据。不要因为 PPL 差、OCRBench 低分或候选低于 690 而暂停，这些都应作为压缩下界证据记录。

默认选择理由：本批直接围绕当前 `wo_r32` 锚点展开局部梯队，能最快判断 683 的掉分归因和 `wo/w2` 的可压边界，同时避免重新展开大规模搜索。

Goal Draft (English-compatible)

/goal In `/home/users/Mayinyi/jikuixie/new-MBQ`, complete the 2026-07-06 No. 01 MBQ experiment batch: centered on the current anchor `wqkv:32, w1:64, w3:64, wo:32, w2:128`, regenerate local-rank variants from the upstream `custom_wo_w2_w3_42_wqkv_r32` cache for `wo_r48`, `wo_r32_w2_r96`, the conditional `wo_r48_w2_r96`, and the optional `wo_r64`; run actual-bit checks, Wikitext2 256-sample PPL, and OCRBench; determine whether `wo_r32` is too aggressive and whether `w2_r96` can join the aggressive low-cost combination; write all valid results and evidence back to `MBQ_DAILY_PLAN.md`.
Verification: inspect the 2026-07-06 plan in `MBQ_DAILY_PLAN.md`, this `tmp.md` goal, `tools/truncate_low_rank_by_module.py`, `tests/test_truncate_low_rank_by_module.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`; confirm `mixed_0.3` is actual bit 2.3963 with OCRBench 701, confirm the current anchor `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r32` is actual bit 2.3176, Delta MiB vs mixed_0.3 -65.47 MiB, OCRBench 683, and confirm the contrast points `42_wqkv_r32_w1_r64_w3_r64` OCRBench 692 and `42_wqkv_r32_w3_r64_wo_r32` OCRBench 692. Generate candidates only from `scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_custom_wo_w2_w3_42_wqkv_r32.pt`, not from the already truncated `wo_r32` cache. Process these candidates in order: `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r48` with semantic ranks `wqkv:32,w1:64,w3:64,wo:48,w2:128`; `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r32_w2_r96` with `wqkv:32,w1:64,w3:64,wo:32,w2:96`; if `wo_r48` clearly repairs the 683 anchor, generate `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r48_w2_r96` with `wqkv:32,w1:64,w3:64,wo:48,w2:96`; only if `wo_r48` is still low but clearly above `wo_r32`, also evaluate `custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r64` with `wqkv:32,w1:64,w3:64,wo:64,w2:128`. For every generated cache, verify `.summary.json` fields including `target_rank_set`, `low_rank_ranks_by_module`, unchanged `linear_bit_map`, unchanged 3-bit module counts, `low_rank_count`, actual bit, and Delta MiB vs `mixed_0.3`. Run Wikitext2 256-sample PPL for every main candidate; treat PPL only as a risk label, not as a hard OCRBench gate. OCRBench must cover the three main candidates, while `wo_r64` is evaluated only if its condition is triggered. For every completed candidate, record cache path, summary path, PPL json/log, OCRBench result txt/results json, actual bit, Delta MiB, rank distribution, 3-bit module counts, OCRBench total and sub-scores, status, and conclusion in `MBQ_DAILY_PLAN.md`.
Constraints: do not rerun full scale search; do not generate `wo_r48/wo_r64` from the `wo_r32` cache because rank32 tensors cannot recover rank48/64; candidates must be built only by offline low-rank truncation from the upstream `custom_wo_w2_w3_42_wqkv_r32.pt` without changing `linear_bit_map`, 3-bit selections, non-target module ranks, model code, core quantization algorithms, dataset logic, baseline results, or unrelated experiment records. PPL is not the primary metric; OCRBench is. No candidate with actual bit greater than or equal to 2.3963 may be treated as a main Pareto candidate. If default `python3` lacks `torch`, use the project `mbq`/conda Python environment for cache generation and tests instead of changing script logic.
Boundaries: read/write scope is limited to `scale_cache/mbq`, `outputs`, `logs`, `MBQ_DAILY_PLAN.md`, `tmp.md`, `tools/truncate_low_rank_by_module.py`, `tests/test_truncate_low_rank_by_module.py`, `tests/test_PPL/2_compute_quant_ppl.sh`, `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and `configs/internvl2/scripts/2_run_quant_eval.sh`. Clearly named current-batch `.pt`, `.summary.json`, PPL logs, OCRBench outputs, and `MBQ_DAILY_PLAN.md` records may be written; `my_eval_ocrbench_svd.yaml` and `2_run_quant_eval.sh` may be adjusted for current-run scale/output/log/GPU settings. Do not write or overwrite unrelated outputs, and do not edit `qmllm`, `main.py`, `main_quant.py`, third-party libraries, model weights, dataset source files, or unrelated configs.
Iteration policy: do discovery and consistency checks first, then process one candidate at a time. Use command shapes like `python3 tools/truncate_low_rank_by_module.py --base-cache scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_custom_wo_w2_w3_42_wqkv_r32.pt --set w1:64 --set w3:64 --set wo:48 --name custom_wo_w2_w3_42_wqkv_r32_w1_r64_w3_r64_wo_r48`; add `--set wo:32 --set w2:96` for `wo_r32_w2_r96`, use `--set wo:48 --set w2:96` for `wo_r48_w2_r96`, and use `--set wo:64` for optional `wo_r64`. After each cache is generated, verify summary, actual bit, target module rank, and non-target module ranks before PPL. Before long tasks, query GPUs with `nvidia-smi --query-gpu=index,name,memory.free,memory.used,utilization.gpu --format=csv`; use PPL command shape `GPU_ID=selected_gpu DATASET=wikitext2 N_SAMPLES=256 SCALE_FILE_NAME=candidate_file.pt bash tests/test_PPL/2_compute_quant_ppl.sh`. Before OCRBench, update `scale_path` and `output_path` in `configs/internvl2/Eval/my_eval_ocrbench_svd.yaml`, and update `LOG_FILE` plus `GPU_ID` in `configs/internvl2/scripts/2_run_quant_eval.sh`; then inspect `outputs/my-internvl2-8b/candidate_name/results/ocrbench_results.txt` and the matching results json. On failure, inspect logs and summaries first; make at most 3 evidence-driven repair rounds, and pause after three repeated infrastructure failures of the same class.
Stop when: the three main candidates `wo_r48`, `wo_r32_w2_r96`, and `wo_r48_w2_r96` have been generated or explicitly reported as conditionally skipped/unsafe, and every generated candidate has summary, actual-bit verification, Wikitext2 PPL, and OCRBench evidence; if the `wo_r64` condition is triggered, it also has the same records or a clear blocker. `MBQ_DAILY_PLAN.md` must contain the 2026-07-06-01 result table, evidence paths, PPL risk labels, OCRBench total and sub-scores, and conclusions for four questions: whether the 683 anchor drop is mainly caused by `wo_r32`, whether safe `wo` rank is around 48 or 64, whether `w2_r96` can be compressed further, and whether the better aggressive combination should be `wqkv:32 + w1:64 + w3:64 + wo:48 + optional w2:96`.
Pause if: new external credentials, paid resources, production data, destructive operations, or user approval to change evaluation thresholds is required; the upstream base cache `scale_cache/mbq/internvl2_8b_w2g32_scale_reweight_true_svd_1.0_custom_wo_w2_w3_42_wqkv_r32.pt` is missing or unreadable; the `low_rank` schema differs from expectations and safe truncation is unclear; actual-bit accounting cannot be verified; tmux/GPU targets cannot be confirmed and long tasks cannot be safely started; dataset download or HuggingFace access fails and existing cache cannot continue; core quantization code changes are required; or the same infrastructure failure repeats three times without new repair evidence. Do not pause because PPL is poor, OCRBench is low, or a candidate scores below 690; those are lower-bound compression evidence and should be recorded.
